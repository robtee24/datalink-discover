# Datalink Discover

Static microsite for the Datalink Discover conference series (HTML, CSS, JS).

Open `index.html` locally or serve the folder with any static file server (for example `python3 -m http.server 8080`).
